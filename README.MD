### Readme
This repository contains the code used for experimental evaluation.

You can find an example of the API documentation in `/openAPI`, while the code is in `/code`.  
For configurations C1 and C2 use `/code/base  `
For configuration C3 use `/code/enriched`